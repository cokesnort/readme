- flash.each do |type, message|
  .alert{ class: { success: 'alert-success', error: 'alert-danger', alert: 'alert-warning', notice: 'alert-info' }[type.to_sym] || type.to_s }
    %button.close{ data: { dismiss: 'alert' } }×
    = message
